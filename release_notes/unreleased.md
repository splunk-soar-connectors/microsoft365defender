**Unreleased**

* Escaped Microsoft Graph values embedded in widget JavaScript contexts.
* Rejected pagination links outside the Microsoft Graph origin.
* URL-encoded incident and alert IDs before inserting them into Graph API paths.
